- @subtitle = "Add New Pre-Survey Results (Part 2)"
- section_and_num_questions = {4 => 2, 5 => 4}

%span
  %p Enter the correct number for each section.
  = form_for @presurvey_part2_fields, :url => {:action => :create} do |f|
    = render :partial => "presurveys/sections", :locals => {:f => f, :section_and_num_questions => section_and_num_questions}
    = render :partial => "presurveys/efficacy", :locals => {:num_questions => 10}
    #submit
      = submit_tag 'Save Changes'
